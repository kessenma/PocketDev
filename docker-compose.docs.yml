services:
  docs:
    build:
      context: .
      dockerfile: apps/docs/Dockerfile
    restart: always
    ports:
      - "${DOCS_PORT:-2222}:2222"
    environment:
      PORT: 2222
      NODE_ENV: production
