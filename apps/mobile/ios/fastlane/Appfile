# The bundle identifier of your app
app_identifier(ENV['APP_IDENTIFIER'] || "run.pocketdev.mobile")

# Your Apple ID email
apple_id(ENV['APPLE_ID'] || "kessen@umich.edu")

# Team ID from Apple Developer Portal
team_id(ENV['TEAM_ID'] || "RHPLRY9X9P")

# iTunes Connect Team ID (numeric, different from Developer Portal alphanumeric ID)
itc_team_id(ENV['ITUNES_CONNECT_TEAM_ID'] || "124074455")
