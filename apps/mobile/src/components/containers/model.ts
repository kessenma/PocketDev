import type {
  ContainerLogsDirection,
  ContainerLogsFilter,
  ContainerLogLine,
  ContainerSummary,
} from '@pocketdev/shared/types'

export type ContainerView = 'containers' | 'logs'

export type {
  ContainerLogsDirection,
  ContainerLogsFilter,
  ContainerLogLine,
  ContainerSummary,
}