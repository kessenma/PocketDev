Add the bundled display font asset here as `PocketDevBauhausDisplay.ttf` when the final licensed font is chosen.

The mobile typography layer currently uses platform-safe condensed fallbacks until that asset is present and linked.
