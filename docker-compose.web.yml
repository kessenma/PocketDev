services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - VITE_APP_URL=${VITE_APP_URL}
      - NODE_ENV=production
