# Dependencies
node_modules/

# Build outputs
dist/
dist-demo/
.turbo/
*.tsbuildinfo

# TanStack Start / Vinxi / Nitro
.vinxi/
.output/
.nitro/

# Environment
.env
.env.local
.env.production

# OS
.DS_Store

# Example apps (reference only, not part of the project)
apps/0-examples/

# Fastlane certs repo (separate git repo, managed by fastlane match)
apps/pocketdev-certs/

# React Native
apps/mobile/ios/build/
apps/mobile/ios/Pods/
apps/mobile/android/build/
apps/mobile/android/app/build/
apps/mobile/android/.gradle/
apps/mobile/vendor/

# Agent bundle (built artifact served by web app)
apps/web/public/agent-bundle.tar.gz

# SQLite (agent local DB)
*.db
*.sqlite

# MCP config (local tool setup)
.mcp.json

# Build logs (deploy script output)
apps/mobile/build-logs/

# IDE
.idea/
*.swp
